import tokenData from './tokens.json';

export const colors = tokenData.colors;
export const typography = tokenData.typography;
export const spacing = tokenData.spacing;
export const radius = tokenData.radius;
export const shadows = tokenData.shadows;
export const tokens = tokenData;
